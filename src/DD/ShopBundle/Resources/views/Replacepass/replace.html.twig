{% extends 'DDShopBundle::layout.html.twig' %}

{% block form_echo -%}
    {% for flashMessage in app.session.flashbag.get('notice') %}
        {{ flashMessage }}
    {% endfor %}
    {{ form(form) }}
{% endblock %}